server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:${server.port}/discovery/

management:
  security:
    enabled: false

#feign:
 # hystrix:
   # enabled: true